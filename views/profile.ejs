<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <!-- Header -->
    <nav class="bg-blue-600 p-4 text-white">
        <div class="container mx-auto flex justify-between">
            <a href="/" class="text-xl font-bold">Chat App</a>
            <div>
                <% if (user) { %>
                    <a href="/profile" class="px-4">Profile</a>
                    <a href="/auth/logout" class="px-4">Logout</a>
                <% } else { %>
                    <a href="/auth/login" class="px-4">Login</a>
                    <a href="/auth/register" class="px-4">Register</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Profile Content -->
    <div class="container mx-auto p-8 text-center">
        <h2 class="text-3xl font-bold">Your Profile</h2>
        <div class="mt-4">
            <img src="<%= user.profilePhoto %>" alt="Profile Photo" class="w-32 h-32 rounded-full mx-auto">
            <form action="/profile/upload-profile-photo" method="POST" enctype="multipart/form-data" class="mt-4">
                <input type="file" name="profilePhoto" required class="border p-2">
                <button type="submit" class="bg-blue-600 text-white px-4 py-2 ml-2 rounded">Update Photo</button>
            </form>
            <h3 class="text-xl mt-4"><%= user.username %></h3>
            <p class="text-gray-600"><%= user.email %></p>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white text-center py-3">
        <p>&copy; 2025 Chat App. All rights reserved.</p>
    </footer>

</body>
</html>
